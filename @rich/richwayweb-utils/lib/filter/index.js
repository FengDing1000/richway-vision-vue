const filterRegion = require(`./filterRegion`);
const filterStation = require(`./filterStation`);
const filterUser = require(`./filterUser`);

module.exports = {
    filterRegion,
    filterStation,
    filterUser
};
